allprojects {
    repositories {
        // Keep mavenLocal at the top to ensure it's searched first
        mavenLocal()
        google()
        mavenCentral()
        jcenter()
        maven { url 'https://jitpack.io' }
        // Remove any reference to private/unavailable repositories
    }

    // Simplify the resolution strategy
    configurations.all {
        resolutionStrategy {
            // Just force using our locally installed version
            force 'com.arthenica:ffmpeg-kit-https:6.0-2.LTS'
        }
    }

    subprojects {
        afterEvaluate { project ->
            if (project.hasProperty('android')) {
                project.android {
                    if (namespace == null) {
                        namespace project.group
                    }
                }
            }
        }
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}

subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
